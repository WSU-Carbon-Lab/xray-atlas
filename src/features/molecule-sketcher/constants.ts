/**
 * Shared height (CSS pixels) for the structure editor canvas and the stored
 * depiction pane in `MoleculeSketcherLab` so the side-by-side panels align.
 */
export const LAB_STRUCTURE_PANE_HEIGHT_PX = 360;

/** Width for database depiction SVG export from the draw lab. */
export const DATABASE_DEPICTION_WIDTH_PX = 480;

/** Height for database depiction SVG export from the draw lab. */
export const DATABASE_DEPICTION_HEIGHT_PX = 360;

/** Shared outer chrome for structure panes in the draw and editor labs. */
export const LAB_STRUCTURE_PANE_CLASS =
  "border-border bg-surface flex w-full max-w-full flex-col overflow-hidden rounded-lg border";
